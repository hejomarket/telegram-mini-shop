import { requireAdminSession } from '../../lib/admin/auth';
import { getDashboardStats } from '../../lib/admin/orders';
import { ORDER_STATUS_LABELS } from '../../lib/admin/status';
import { formatRupiah } from '../../lib/format';
export default async function AdminPage(){ await requireAdminSession(); let stats; try{stats=await getDashboardStats();}catch{ return <section className="rounded-3xl bg-white p-6 shadow-card"><h1 className="text-2xl font-black">Dashboard</h1><p className="mt-3 text-red-700">Gagal memuat data dashboard.</p></section> } const cards=[['Total Pesanan',stats.total],['Menunggu Konfirmasi',stats.byStatus.pending],['Dikonfirmasi',stats.byStatus.confirmed],['Sedang Diproses',stats.byStatus.processing],['Dikirim',stats.byStatus.shipped],['Selesai',stats.byStatus.completed],['Dibatalkan',stats.byStatus.cancelled],['Total Nilai Pesanan',formatRupiah(stats.revenue)]]; return <section><h1 className="text-3xl font-black">Dashboard</h1><p className="mt-2 text-soia-green/70">Ringkasan operasional pesanan SOIA. Total Nilai Pesanan mengecualikan pesanan dibatalkan.</p><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([label,value])=><article key={label} className="rounded-3xl bg-white p-5 shadow-card"><p className="text-sm font-bold text-soia-green/65">{label}</p><p className="mt-2 text-3xl font-black">{value}</p></article>)}</div></section> }
