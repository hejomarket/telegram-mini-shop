import { NextResponse } from 'next/server';
import { verifyOrderAccessToken } from '../../../../../lib/orders/access';
import { CUSTOMER_ORDER_TOKEN_HEADER } from '../../../../../lib/orders/customer';
import { findOrder, getOrderAccessTokenHash } from '../../../../../lib/orders/repository';
import { canRetryPayment } from '../../../../../lib/midtrans/status';
export async function GET(request: Request,{params}:{params:Promise<{orderNumber:string}>}){ const {orderNumber}=await params; const token=new URL(request.url).searchParams.get('accessToken') ?? request.headers.get(CUSTOMER_ORDER_TOKEN_HEADER) ?? ''; const hash=await getOrderAccessTokenHash(orderNumber); if(!hash || !verifyOrderAccessToken(token,hash)) return NextResponse.json({success:false,message:'Pesanan tidak dapat diakses dari perangkat ini.'},{status:403}); const {order}=await findOrder(orderNumber); if(!order) return NextResponse.json({success:false,message:'Pesanan tidak dapat diakses dari perangkat ini.'},{status:403}); return NextResponse.json({success:true,orderNumber:order.orderNumber,orderStatus:order.status,paymentStatus:order.paymentStatus,paymentType:order.paymentType,grandTotal:order.grandTotal,createdAt:order.createdAt,paidAt:order.paidAt,expiryTime:order.midtransExpiryTime,canRetry:canRetryPayment(order.paymentStatus)}); }
