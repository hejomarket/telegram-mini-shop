import type { Product } from '../lib/products';
export function ProductVisual({product}:{product:Product}){return <div className="relative h-40 overflow-hidden rounded-[2rem] bg-gradient-to-br from-soia-cream to-white p-5"><div className="absolute -right-8 -top-8 h-28 w-28 rounded-full opacity-30" style={{background:product.accent}}/><div className="absolute bottom-4 right-5 h-10 w-10 rounded-full border-8 border-soia-green/10"/><div className="mx-auto flex h-32 w-24 rotate-[-3deg] flex-col items-center justify-between rounded-2xl border-2 border-soia-green bg-white p-3 shadow-xl"><span className="rounded-full px-2 py-1 text-[10px] font-bold text-white" style={{background:product.accent}}>{product.badge}</span><strong className="text-center text-lg leading-5 text-soia-green">{product.name.replace('SOIA ','')}</strong><span className="text-[10px] font-semibold uppercase tracking-widest text-soia-leaf">SOIA</span></div></div>}
